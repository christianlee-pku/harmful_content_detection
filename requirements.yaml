name: hcd
channels:
  - pytorch
  - nvidia
  - defaults
dependencies:
  - python=3.9
  - pytorch>=2.0.0
  - torchvision
  - torchaudio
  # - pytorch-cuda=11.8
  - numpy
  - pip
  - pip:
    - transformers>=4.30.0
    - boto3>=1.26.0
    - onnxruntime>=1.15.0
    - Pillow
    - tqdm
    - pyyaml
    - requests
    - pytest
    - black
    - ruff
